// write a c program that accpts two integers from the user and calculates the sum of two integers

#include <stdio.h>

int main()
{
    int a, b;
    scanf("%d %d", &a, &b);
    printf("%d", a + b);
    return 0;
}